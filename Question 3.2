void main(void)
{
	my_age.day = 26;
	my_age.month = 02;
	my_age.year = 2001;
	my_age.age = 22;

	init_LCD();
	char string[3];

	while(1)
	{
		for(int i = 0; i <= my_age.age; i++){
			sprintf(string, "%d", i);
			lcd_putstring(string);
			delay(100000);
			lcd_command(CLEAR);
		}
	}
}
